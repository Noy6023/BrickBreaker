using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace BrickBreaker
{
    static class Constants
    {
        public static Vector BAT_SIZE = new Vector(300, 20);
        public static Vector DEFULT_SCREEN_SIZE = new Vector(1000, 2000);
        public static Vector BRICK_SIZE = new Vector(75, 50);
        public const int SPACE = 75;
        public const int NUM_OF_PLAYERS = 5;


    }
}